commands
start -> give the list of tasks and ask a number
start + int  (<= nb task)-> start the task
start + int (> nb tasks) -> error task doesn't exist + list of task
start + string (= name of an existing task) -> start the task

start + string (!=name of an existing task) -> ask to create the task (yes|no)
   + todo??

if lastcommand == start
int (<= nb task)-> start the task

